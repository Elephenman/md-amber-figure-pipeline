# an3 — 关键氢键距离时序（fig22 数据源; 由 config HBOND_PAIRS 决定键清单）
# 每键一行 distance, 输出 {RAW}/hbd_<name>.dat; 与 an2b 的 hbond series 不同, 本文件逐键可辨
# 模板占位符: {PARM7} {TRAJIN_BLOCK} {HB_LINES}
parm {PARM7}
{TRAJIN_BLOCK}
{HB_LINES}
run
quit
